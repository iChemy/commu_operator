[project]
name = "commu-operator-client"
version = "0.1.0"
description = "Client CLI for operating CommU over TCP."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "audioop-lts>=0.2.2",
    "pydantic>=2.13.3",
    "pydub>=0.25.1",
]
